# Copy to key.properties (ignored by Git) and enter your upload key details.
# Use forward slashes for an absolute Windows path.
storeFile=C:/path/to/upload-keystore.jks
storePassword=REPLACE_ME
keyAlias=upload
keyPassword=REPLACE_ME
